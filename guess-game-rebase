import random

# Программа загадывает число от 1 до 100
secret_number = random.randint(1, 100)
attempts = 0

print("Я загадал число от 1 до 100. Попробуй угадать!")

while True:
    try:
        guess = int(input("Введите число: "))
        attempts += 1

        difference = abs(secret_number - guess)

        if guess < secret_number:
            if difference < 10:
                print("Тепло! Нужно чуть больше.")
            else:
                print("Холодно! Намного больше.")
        elif guess > secret_number:
            if difference < 10:  # НОВАЯ СТРОКА
                print("Тепло! Нужно чуть меньше.")  # НОВАЯ СТРОКА
            else:
                print("Холодно! Намного меньше.")
        else:
            print(f"Ура! Ты угадал число {secret_number} за {attempts} попыток!")
            break
    except ValueError:
        print("Пожалуйста, введи целое число.")
