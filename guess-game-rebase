import random

secret_number = random.randint(1, 100)
attempts = 0
max_attempts = 5

print("Я загадал число от 1 до 100. Попробуй угадать!")
print(f"У тебя {max_attempts} попыток!")

while attempts < max_attempts:
    try:
        guess = int(input("Введите число: "))
        attempts += 1
        remaining = max_attempts - attempts
        difference = abs(secret_number - guess)
        
        if guess < secret_number:
            if difference < 10:
                print(f"Тепло! Нужно чуть больше. Осталось попыток: {remaining}")
            else:
                print(f"Холодно! Намного больше. Осталось попыток: {remaining}")
        elif guess > secret_number:
            if difference < 10:
                print(f"Тепло! Нужно чуть меньше. Осталось попыток: {remaining}")
            else:
                print(f"Холодно! Намного меньше. Осталось попыток: {remaining}")
        else:
            print(f"Ура! Ты угадал число {secret_number} за {attempts} попыток!")
            break
    except ValueError:
        print("Пожалуйста, введи целое число.")
else:
    print(f"Попытки кончились! Было загадано {secret_number}")
